# Canonical 2014 Subclasses Catalog

This directory is the canonical normalized subclass catalog for 5e 2014.

- One subclass payload per `*.json` file.
- `content_id` must follow `subclass:<subclass_id>_<class_id>|<SOURCE>`.
- Subclass records define subclass-specific feature grants keyed to class level thresholds.

